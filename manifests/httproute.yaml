apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bakseter-no
  namespace: bakseter-no
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: cloudflared
      namespace: cloudflared

  hostnames:
    - bakseter.no

  rules:
    - matches:
        - path:
            type: Exact
            value: /
        - path:
            type: Exact
            value: /favicon.ico
        - path:
            type: Exact
            value: /status

      backendRefs:
        - group: ''
          kind: Service
          name: bakseter-no
          port: 80
          weight: 1
